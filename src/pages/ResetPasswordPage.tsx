import ClientLayout from "../layouts/client/ClientLayout";
import ResetPasswordForm from "../components/common/reset-password/ResetPasswordForm";

const ResetPasswordPage = () => {
  return (
    <ClientLayout>
      <ResetPasswordForm />
    </ClientLayout>
  );
};

export default ResetPasswordPage;
